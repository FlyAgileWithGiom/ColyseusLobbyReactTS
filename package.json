{
  "name": "colyseus-teams-project",
  "version": "1.0.0",
  "description": "component for a react lobby for colyseus multiplayer, client and server package",
  "workspaces": [
    "client/package/colyseus-teams-lobby-react.js",
    "client/example"
  ],
  "private": true,
  "scripts": {
    "client:example:install": "npm --prefix client/example install",
    "example:server:install": "npm --prefix example/server install",
    "example:install": "npm run example:client:install && npm run example:server:install",
    "example:start:dev": "concurrently \"npm run --prefix example/server start:dev\" \"npm run --prefix client/example start:dev\"",
    "build:all": "npm run build:client && npm run build:server",
    "client:install": "npm --prefix client/package/* install",
    "install:server": "npm --prefix components/lobby/server install",
    "install:all": "npm run install:client && npm run install:server",
    "client:build": "npm --prefix client/package/* run build",
    "build:server": "npm --prefix components/lobby/server run build",
    "client:example:build": "npm --prefix client/example run build",
    "link:client": "npm --prefix components/lobby/client link",
    "link:server": "npm --prefix components/lobby/server link",
    "link:all": "npm run link:client && npm run link:server",
    "link:example": "npm --prefix example/client link colyseus-teams-lobby-react.js && npm --prefix example/server link colyseus-teams-lobby",
    "clean:client": "npm --prefix components/lobby/client run clean",
    "clean:server": "npm --prefix components/lobby/server run clean",
    "clean:all": "npm run clean --workspaces",
    "clean:example": "npm --prefix client run clean && npm --prefix example/server run clean",
    "clean:hard": "rm -rf node_modules && rm -rf components/lobby/client/node_modules && rm -rf components/lobby/server/node_modules && rm -rf example/client/node_modules && rm -rf example/server/node_modules"
  }
}
